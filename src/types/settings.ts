/**
 * Custom data to save via electron-store
 * Should be related to the app and kind of lightweight
 * (i.e. user preferences, etc.)
 *
 * Other heavier data should be saved by using a different method such as
 * a database, binary files, etc.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface Settings {}
